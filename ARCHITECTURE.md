# 智能选股平台 — 系统架构设计

> 版本: v1.1 | 创建日期: 2026-05-04 | 更新: 三层漏斗 + 板块轮动 + Token 优化

---

## 一、整体架构：三层漏斗筛选

```
┌─────────────────────────────────────────────────────┐
│  漏斗 L1: 一票否决过滤（规则引擎，零 LLM 调用）       │
│  → 过滤 ST/*ST、亏损、高负债、处罚等硬伤股              │
│  → A 股 ~5400 只 → 剩余 ~3000-4000 只                 │
├─────────────────────────────────────────────────────┤
│  漏斗 L2: 量化评分排序（规则引擎，零 LLM 调用）         │
│  → 按评分模型打分（基本面50% + 技术面30% + 资金面20%） │
│  → 按板块分组，每板块取 Top 10                         │
│  → 输出候选池 ~50-100 只                              │
├─────────────────────────────────────────────────────┤
│  漏斗 L3: 用户按需深度分析（LLM 调用）                 │
│  → 用户点击某只股票 → 触发大模型生成详细报告             │
│  → 包含评分过程、指标解读、买卖点建议                    │
│  → 按实际调用计费，极大节省 Token                       │
└─────────────────────────────────────────────────────┘
```

---

## 二、L1 漏斗：一票否决过滤（纯规则引擎）

**触发条件，直接排除，不进入后续评分：**

| # | 条件 | 数据来源 | 实现方式 |
|---|------|----------|----------|
| 1 | ST / *ST 股票 | 交易所股票列表 | 名称含 "ST" 或 status=ST |
| 2 | 近2年连续亏损 | 财报净利润 | `净利润_t < 0 AND 净利润_t-1 < 0` |
| 3 | 经营现金流持续为负 | 现金流量表 | `经营现金流_t < 0 AND 经营现金流_t-1 < 0` |
| 4 | 资产负债率 > 80%（非金融） | 资产负债表 | `资产负债率 > 0.8 AND 行业 != 银行/保险/券商` |
| 5 | 近1年被证监会处罚/立案调查 | 公告/新闻 | NLP 提取公告关键词 |
| 6 | 大股东质押比例 > 50% | 股东数据 | `质押股数 / 持股总数 > 0.5` |
| 7 | 近3月有大额减持计划 | 公告 | 减持公告时间 <= 90天 |
| 8 | 日均成交额 < 500万（流动性差） | 行情数据 | `20日均成交额 < 500万` |

**实现方案：** 定时任务（每天收盘后）执行 SQL/Python 脚本，结果存入 `filtered_out` 表，次日直接使用缓存。

---

## 三、L2 漏斗：量化评分 + 板块分组（纯规则引擎）

### 3.1 评分模型

#### 基本面评分（50分）
```python
def score_fundamental(stock):
    score = 0
    if stock.roe > 0.15:          score += 10
    if stock.profit_growth_3y > 0.10: score += 10  # 连续3年增长>10%
    if stock.pe_percentile < 0.4:  score += 8   # PE 处于行业前40%分位
    if stock.operating_cashflow > 0: score += 7
    if stock.debt_ratio < 0.50:    score += 7
    if stock.gross_margin > 0.30:  score += 5
    if stock.has_dividend:         score += 3
    return score  # max 50
```

#### 技术面评分（30分）
```python
def score_technical(stock):
    score = 0
    if stock.ma_bullish:           score += 8   # 均线多头排列
    if stock.macd_golden_cross:    score += 7   # MACD金叉
    if 30 < stock.rsi < 70:        score += 5   # RSI 健康区间
    if stock.vol_ratio > 1.5:      score += 5   # 放量
    if stock.price > stock.ma20:   score += 5   # 站上20日均线
    return score  # max 30
```

#### 资金面评分（20分）
```python
def score_capital(stock):
    score = 0
    if stock.net_inflow_5d > 0:    score += 8   # 主力近5日净流入
    if stock.margin_balance_up:    score += 6   # 融资余额趋势上升
    if stock.rating_buy_ratio > 0.5: score += 6 # 机构"买入/增持"占比
    return score  # max 20
```

#### 总分与评级
```
总分 = 基本面 × 1.0 + 技术面 × 1.0 + 资金面 × 1.0  (满分100)

评级:
  ≥ 80 → A+ 优质
  65-79 → A 良好
  50-64 → B 一般
  < 50  → C 较弱
```

### 3.2 板块轮动分组

**核心逻辑：按热门板块分组，每组选 Top 10，避免单一板块集中。**

#### 板块识别
```python
# 板块分类（按申万一级行业 + 概念板块）
SECTORS = {
    '科技': ['半导体', '芯片', 'AI算力', '通信', '消费电子', '软件'],
    '新能源': ['锂电池', '光伏', '风电', '储能', '新能源车'],
    '医药': ['创新药', 'CXO', '医疗器械', '中药', '生物制品'],
    '消费': ['白酒', '食品饮料', '家电', '零售', '旅游'],
    '金融': ['银行', '保险', '券商', '多元金融'],
    '制造': ['军工', '高端装备', '机器人', '航空航天'],
    '周期': ['钢铁', '煤炭', '化工', '有色金属', '建材'],
    '地产基建': ['房地产', '建筑工程', '建材', '装修'],
    # ... 可根据市场情况动态调整
}
```

#### 板块热度评分（判断哪些板块当前热门）
```python
def sector_heat(sector_stocks):
    """
    综合以下因素计算板块热度:
    1. 板块近5日涨幅
    2. 板块成交额占比变化
    3. 板块内涨停股数量
    4. 板块资金净流入
    5. 板块新闻/搜索热度
    """
    return heat_score  # 0-100
```

#### 选股流程
```python
def select_top_stocks():
    # 1. 获取所有通过 L1 过滤的股票
    candidates = get_filtered_stocks()
    
    # 2. 计算每只股票的评分
    for stock in candidates:
        stock.total_score = (
            score_fundamental(stock) * 0.50 +
            score_technical(stock) * 0.30 +
            score_capital(stock) * 0.20
        )
    
    # 3. 计算板块热度，选出当前热门板块（Top 5-8 个）
    sector_scores = {}
    for sector, sector_name in SECTORS.items():
        sector_stocks = [s for s in candidates if s.sector in sector_name]
        if sector_stocks:
            heat = sector_heat(sector_stocks)
            sector_scores[sector] = {
                'heat': heat,
                'stocks': sorted(sector_stocks, key=lambda x: x.total_score, reverse=True)[:10]
            }
    
    # 4. 按热度排序，取 Top N 个板块
    hot_sectors = sorted(sector_scores.items(), key=lambda x: x[1]['heat'], reverse=True)[:6]
    
    # 5. 每个热门板块取 Top 10，得到最终候选 ~60 只
    final_pool = []
    for sector_name, data in hot_sectors:
        final_pool.extend(data['stocks'])
    
    return final_pool  # 最多 ~60 只
```

### 3.3 买卖点计算（规则引擎）

```python
def calculate_trade_points(stock):
    """
    基于技术指标计算合理买卖价格区间
    """
    # 买入区间
    buy_support = max(
        stock.ma20 * 0.98,       # 20日均线附近
        stock.boll_lower,         # 布林下轨
        stock.recent_low * 1.02   # 近期低点上方
    )
    
    # 卖出区间
    sell_resistance = min(
        stock.ma60 * 1.05,        # 60日均线附近
        stock.boll_upper,         # 布林上轨
        stock.recent_high * 0.98  # 近期高点下方
    )
    
    # 止盈止损
    stop_profit = stock.entry_price * 1.15   # 15%止盈
    stop_loss = stock.entry_price * 0.92     # 8%止损
    
    return {
        '买入区间': f"{buy_support:.2f} - {(buy_support * 1.03):.2f}",
        '卖出区间': f"{sell_resistance:.2f} - {(sell_resistance * 1.03):.2f}",
        '止盈价': f"{stop_profit:.2f}",
        '止损价': f"{stop_loss:.2f}",
        '盈亏比': f"{(sell_resistance - buy_support) / (buy_support - stop_loss):.1f}:1"
    }
```

---

## 四、L3 漏斗：按需深度分析（LLM 调用）

### 4.1 触发场景
- 用户点击某只股票，查看"详细分析报告"
- 用户要求对比多只股票
- 用户自定义条件筛选后，需要解释推荐理由

### 4.2 LLM Prompt 模板
```
你是一位资深股票分析师。请对以下股票进行详细分析：

股票: {stock_code} - {stock_name}
当前价格: {price}
综合评分: {total_score} / 评级: {rating}

【基本面数据】
- PE: {pe}, PB: {pb}, ROE: {roe}%
- 营收增长率: {revenue_growth}%, 净利润增长率: {profit_growth}%
- 毛利率: {gross_margin}%, 资产负债率: {debt_ratio}%
- 经营现金流: {operating_cashflow} 亿

【技术面数据】
- 均线: {ma_status}
- MACD: {macd_status}
- RSI: {rsi}
- 近5日成交量趋势: {vol_trend}

【资金面数据】
- 主力近5日净流入: {net_inflow} 万
- 融资余额趋势: {margin_trend}

请输出：
1. 综合点评（3-5句话）
2. 各项指标的评分明细和解读
3. 风险提示
4. 操作建议（适合长线/短线/观望）
```

### 4.3 Token 消耗估算
```
单次 L1+L2 过滤评分: 0 次 LLM 调用
单次 L3 详细分析: ~1 次 LLM 调用
  - 输入: ~800 tokens
  - 输出: ~1200 tokens
  - 合计: ~2000 tokens/次

如果 100 个用户每天各看 3 只股票:
  = 300 次 × 2000 tokens = 600,000 tokens/天
  
对比全量 LLM 分析:
  5400 只 × 2000 tokens = 10,800,000 tokens/天
  
节省: ~94%
```

---

## 五、系统模块划分

```
┌──────────────────────────────────────────────────┐
│                   前端展示层                       │
│  板块热力图 → 股票列表 → 详情报告 → 自选/预警      │
├──────────────────────────────────────────────────┤
│                   API 服务层                       │
│  选股接口 / 评分接口 / 详情接口 / 预警接口          │
├──────────────┬──────────────┬────────────────────┤
│  L1 过滤引擎  │  L2 评分引擎  │  L3 LLM 分析引擎    │
│  (规则+SQL)   │  (规则+计算)   │  (按需调用)         │
├──────────────┴──────────────┴────────────────────┤
│                   数据层                          │
│  行情DB / 财务DB / 资金流向DB / 缓存Redis           │
├──────────────────────────────────────────────────┤
│                   数据源                          │
│  AKShare / Tushare / 东方财富 / 同花顺 API          │
└──────────────────────────────────────────────────┘
```

---

## 六、定时任务调度

```python
# 每日调度 (收盘后执行)
SCHEDULE = {
    '15:30': '更新行情数据 + L1 过滤',
    '16:00': '计算全量评分 + L2 排序',
    '16:30': '计算板块热度 + 分组 Top 10',
    '17:00': '生成每日选股日报',
    '20:00': '检查预警条件 (止盈/止损/异动)',
}

# 盘中实时更新 (每5分钟)
REALTIME = {
    'interval': '5min',
    'tasks': [
        '更新实时行情',
        '检查技术面条件变化 (均线突破/放量等)',
        '检查资金异动',
    ]
}
```

---

## 七、行业适配阈值调整

不同行业需调整评分阈值（L2 评分引擎内处理）：

| 行业 | PE 阈值调整 | 重点关注指标 |
|------|------------|-------------|
| 银行/保险 | PE 5-15 | PB, ROE, 不良率 |
| 地产 | 不适用 PE | 负债率, 销售额, 土储 |
| 科技/成长 | PE 容忍 50+ | 营收增速, PEG, 研发占比 |
| 消费/医药 | 正常 | 毛利率, 品牌护城河 |
| 周期股 | 低 PE 可能陷阱 | 行业周期位置, 产品价格 |

---

## 八、前端 Web UI 设计

### 8.1 技术选型

| 层级 | 方案 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | 轻量、响应式、组件化 |
| UI 组件库 | Element Plus / Ant Design Vue | 表格、卡片、图表等成熟组件 |
| 图表库 | ECharts | K线、柱状图、热力图、雷达图 |
| 状态管理 | Pinia | 轻量级状态管理 |
| HTTP 客户端 | Axios | 统一 API 请求封装 |
| 后端框架 | FastAPI (Python) | 异步高性能，与评分引擎同源 |
| 访问方式 | 浏览器 IP:端口 直接访问 | 无需域名，内网部署即用 |

### 8.2 页面布局设计

```
┌─────────────────────────────────────────────────────────────────┐
│  🦐 智能选股平台                           [🔍搜索] [⚙️设置] [👤用户] │
├─────────────────────────────────────────────────────────────────┤
│  📊 今日大盘概况  │  上证指数 3,XXX ▲X.X%  │  成交额 X.XX万亿       │
│  2026-05-04 15:00  │  深证成指 XX,XXX ▲X.X% │  涨跌比 XXX:XXX      │
├─────────────────────────────────────────────────────────────────┤
│  ┌─ 📅 最近分析: 2026-05-04 16:30 ────────── [🔄 立即分析] ─┐  │
│  │  ⏰ 下次定时分析: 2026-05-05 16:00   [⏱️定时设置]          │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🏷️ 板块热度排行 (拖拽排序 / 按热度自动排列)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐│
│  │ 🔥 科技  │ │ 🔥 新能源 │ │ 🔥 医药   │ │ 🔥 消费   │ │ 🔥 金融   ││
│  │ ████ 92 │ │ ███ 78  │ │ ███ 75  │ │ ██ 65   │ │ ██ 60   ││
│  │ ↑ +5.2% │ │ ↑ +3.1% │ │ ↓ -0.8% │ │ ↑ +1.5% │ │ ↑ +2.0% ││
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘│
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📋 板块选股详情 —— 点击板块名称展开 / 切换                       │
│                                                                 │
│  ═══ 🔥 科技板块 (热度 92) ▲5.2% ═══                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 排名 │ 代码   │ 名称     │ 现价  │ 涨跌幅 │ 评分 │ 评级 │ 买卖区间    │ 操作 ││
│  │──────│────────│──────────│───────│────────│──────│──────│───────────│──────││
│  │  1   │688008  │澜起科技  │65.20  │ +3.2% │  86  │ A+  │ 62-65 / 72  │ 详情 ││
│  │  2   │300782  │卓胜微    │112.50 │ +1.8% │  82  │ A+  │ 108-110/125 │ 详情 ││
│  │  3   │603986  │兆易创新  │98.30  │ -0.5% │  78  │ A   │ 94-96  / 108│ 详情 ││
│  │  4   │002475  │立讯精密  │32.80  │ +2.1% │  75  │ A   │ 31-32  / 37 │ 详情 ││
│  │  5   │600584  │长电科技  │28.50  │ +4.5% │  73  │ A   │ 27-28  / 32 │ 详情 ││
│  │ ...  │        │          │       │        │      │      │             │      ││
│  │ 10   │300661  │圣邦股份  │75.60  │ +0.9% │  65  │ A   │ 72-74  / 82 │ 详情 ││
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ═══ 🔥 新能源板块 (热度 78) ▲3.1% ═══                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 排名 │ 代码   │ 名称     │ 现价  │ 涨跌幅 │ 评分 │ 评级 │ 买卖区间    │ 操作 ││
│  │──────│────────│──────────│───────│────────│──────│──────│───────────│──────││
│  │  1   │300750  │宁德时代  │210.30 │ +2.8% │  85  │ A+  │ 205-208/235 │ 详情 ││
│  │ ...  │        │          │       │        │      │      │             │      ││
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  📈 市场总览 (底部快捷面板)                                      │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │ A+ 优质: 12 │ │ A  良好: 28│ │ 涨停股: 85  │ │ 北向流入:  │   │
│  │            │ │            │ │            │ │   +52.3亿   │   │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 核心页面/组件

#### 8.3.1 首页仪表盘 (Dashboard)
- **大盘概览栏**: 上证/深证/创业板指数、涨跌比、成交额、涨跌家数
- **分析状态栏**: 最近分析时间、下次定时分析时间、当前分析状态 (空闲/分析中/已完成)
- **板块热度卡片**: 横向可滚动卡片，按热度排序，显示板块名称、热度值、涨跌幅
- **板块股票列表**: 展开当前板块的 Top 10 股票，表格展示
- **底部统计**: A+/A/B/C 各评级数量、涨停数、北向资金等快捷统计

#### 8.3.2 个股详情页 (点击"详情"弹出)
- **侧边抽屉/弹窗** 展示:
  - 股票基本信息 (代码、名称、现价、涨跌幅、所属行业)
  - 评分雷达图 (基本面/技术面/资金面)
  - 各项指标明细 (PE、ROE、毛利率等)
  - 买卖点建议 (买入区间、卖出区间、止盈止损价)
  - K线走势图 (ECharts 绘制)
  - [📊 AI 深度分析] 按钮 → 调用 L3 LLM 生成详细报告

#### 8.3.3 设置页
- **定时分析配置**:
  - 开启/关闭每日定时分析
  - 设置分析时间 (默认收盘后 16:00)
  - 设置通知方式 (飞书消息/站内通知)
- **板块配置**:
  - 自定义板块分类
  - 调整各板块权重
  - 设置热门板块数量 (默认 6 个)
- **评分权重配置**:
  - 调整基本面/技术面/资金面权重比例
  - 设置各指标阈值
- **预警设置**:
  - 止盈/止损比例
  - 异动监控 (放量、涨停、跌停)

### 8.4 分析触发机制

#### 8.4.1 手动触发分析
```
用户点击 [🔄 立即分析] 按钮
    ↓
前端 POST /api/analyze?trigger=manual
    ↓
后端返回 task_id，开始分析
    ↓
前端轮询 GET /api/analyze/status/{task_id} (或 WebSocket 推送)
    ↓
分析完成 → 前端刷新页面数据
    ↓
显示 "分析完成: 2026-05-04 14:30 | 耗时 2m15s"
```

**分析进度实时展示:**
```
┌─ 正在分析中... ────────────────────────────────┐
│  Step 1/3 ✅ L1 一票否决过滤 (完成: 3.2s)        │
│  Step 2/3 🔄 L2 量化评分 (进度: ████████░░ 78%) │
│  Step 3/3 ⏳ 板块分组排序                        │
│                                                  │
│  已处理: 4,230 / 5,400 只股票                    │
│  预计剩余: ~45秒                                 │
└──────────────────────────────────────────────────┘
```

#### 8.4.2 每日定时分析
```
APScheduler / Celery Beat 定时任务
    ↓
每天 16:00 自动触发 (可配置)
    ↓
执行完整 L1 → L2 → 板块分组 流程
    ↓
结果写入数据库
    ↓
可选: 推送飞书通知 "今日选股分析已完成，共筛选出 12 只 A+ 优质股"
```

**定时任务配置页:**
```
⏰ 定时分析设置
┌──────────────────────────────────────┐
│  ☑️ 开启每日定时分析                  │
│  ⏰ 执行时间: [16:00]                │
│  📅 执行日期: [周一 ~ 周五] (交易日)  │
│  📢 完成后通知: ☑️飞书  ☐邮件  ☐站内  │
│  [保存设置]                          │
└──────────────────────────────────────┘
```

### 8.5 API 接口设计

#### 数据查询接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/dashboard` | GET | 获取首页大盘数据、板块热度、统计信息 |
| `/api/sectors` | GET | 获取所有板块列表及热度排名 |
| `/api/sectors/{name}/stocks` | GET | 获取指定板块 Top 10 股票列表 |
| `/api/stocks/{code}` | GET | 获取单只股票评分和指标数据 |
| `/api/stocks/{code}/chart` | GET | 获取 K 线数据 (用于图表渲染) |
| `/api/stocks/{code}/trade-points` | GET | 获取买卖点建议 |

#### 分析控制接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/analyze` | POST | 手动触发分析 (query: trigger=manual) |
| `/api/analyze/status/{task_id}` | GET | 查询分析任务进度 |
| `/api/analyze/history` | GET | 查看历史分析记录 |
| `/api/schedule` | GET/PUT | 查看/修改定时分析配置 |

#### AI 深度分析接口 (LLM)
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/analyze/ai/{code}` | POST | 调用 LLM 生成个股深度报告 |
| `/api/analyze/ai/compare` | POST | 多只股票对比分析 |

### 8.6 响应式设计

- **桌面端 (≥1200px)**: 完整布局，板块卡片横向排列，列表全宽显示
- **平板端 (768-1199px)**: 板块卡片两行排列，表格可横向滚动
- **移动端 (<768px)**: 板块卡片纵向堆叠，表格简化为卡片式展示

### 8.7 部署方案

```
服务器部署 (单台机器即可):
┌─────────────────────────────────────┐
│  Nginx (可选)                        │
│  ├─ 静态文件: /dist (Vue 构建产物)   │
│  └─ 反向代理: /api → FastAPI         │
│                                      │
│  FastAPI (Python 后端)               │
│  ├─ 端口: 8000                       │
│  ├─ 数据引擎: L1/L2 评分             │
│  └─ 调度器: APScheduler              │
│                                      │
│  Redis (缓存)                        │
│  ├─ 评分结果缓存                     │
│  └─ 实时行情缓存                     │
│                                      │
│  SQLite / PostgreSQL (数据库)        │
│  ├─ 股票基础信息                     │
│  ├─ 历史评分记录                     │
│  └─ 用户设置                         │
└─────────────────────────────────────┘

访问: http://<服务器IP>:8000 或 http://<服务器IP>:80 (经 Nginx)
```

### 8.8 页面交互流程图

```
用户打开浏览器 → http://IP:端口
    ↓
首页加载 (从缓存读取最新分析结果)
    ↓
┌──────────────────────────────────────┐
│ 用户看到:                              │
│ 1. 大盘指数 + 分析状态                  │
│ 2. 板块热度排行 (可左右滑动)             │
│ 3. 当前板块 Top 10 股票列表             │
└──────────────────────────────────────┘
    ↓
用户操作:
├─ 点击板块卡片 → 切换到该板块股票列表
├─ 点击股票 "详情" → 弹出侧边抽屉
│   ├─ 查看评分雷达图、指标明细
│   ├─ 查看 K 线走势、买卖点
│   └─ 点击 "AI 深度分析" → LLM 生成报告
├─ 点击 [🔄 立即分析] → 触发手动分析
│   └─ 显示分析进度 → 完成后刷新
├─ 点击 [⏱️ 定时设置] → 进入设置页
│   └─ 配置定时时间、通知方式、评分权重
└─ 搜索框输入代码/名称 → 快速定位股票
```

---

*文档版本: v1.2 | 更新日期: 2026-05-04*
*核心变更: 三层漏斗架构 + 板块轮动分组 + Token 优化策略 + 买卖点计算 + Web UI 前端设计 + 手动/定时分析机制*
