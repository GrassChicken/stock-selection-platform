# 智能选股平台 — 模块化迭代开发计划

> 版本: v4.0 | 创建日期: 2026-05-05 | 目标端口: **5100**
> 策略: 小模块快速迭代，边开发边调试

---

## 当前状态 (2026-05-05)

| 模块 | 状态 | 说明 |
|------|------|------|
| M1 API 修复 | ✅ 完成 | 路由/导入/启动 |
| M2 后端 API 真实引擎 | ✅ 完成 | dashboard/stocks/sectors 全通 |
| M3 财务数据适配 | ✅ 完成 | 字段映射 + ROE 年化 |
| M4 前端状态层 | ✅ 完成 | Pinia + API 封装 + Dashboard 接真实数据 |
| M6 定时任务 + 通知 | ✅ 完成 | 引擎接入 + 交易日判断 + 飞书通知 |

---

## 迭代清单

### M1: 修复 API 尾斜杠 + 启动验证 ✅
- [x] 所有路由 `/` 改为 `""` 兼容无尾斜杠
- [x] `get_stock_info` → `get_stock_basic_info` 导入修复
- [x] 后端重启成功，所有 API 返回 200

### M2: 后端 API 接入真实引擎 ✅
- [x] `/api/dashboard` 三大指数 + 涨跌家数 + 成交额 + 板块
- [x] `/api/stocks/{code}` 实时评分 + K线 + 买卖点
- [x] `/api/sectors` 板块列表 + 股票详情
- [x] pipeline 财务字段映射 + numpy 类型清洗

### M3: AKShare 财务数据适配 ✅
- [x] K线列名映射 (中文→英文)
- [x] 财务字段映射函数 (`_map_fund` / `_parse_val`)
- [x] ROE 年化修正 (Q1×4, Q2×2, Q3×1.33)

### M4: 前端状态层 + API 封装 ✅
- [x] Axios 请求封装 + 拦截器
- [x] Pinia 状态管理 (dashboard store)
- [x] Dashboard.vue 接入真实数据

### M5: 前端 UI 优化 + 响应式 ✅

#### M5.1 主板/创业板分页签 ✅
- [x] pipeline 增加 `board` 字段 (主板/创业板/科创板)
- [x] `/api/sectors?board=main|chinext|star` 支持过滤
- [x] Dashboard Tab 切换组件

#### M5.2 响应式布局 ✅
- [x] CSS Media Query: 桌面(>1024px) / 平板(≤1024px) / 手机(≤768px) / 小屏(≤480px)
- [x] 指数卡片: 4列 → 手机2×2
- [x] 板块卡片: 横排滚动 → 手机纵向堆叠
- [x] 股票表格: 完整表格 → 手机卡片式列表

#### M5.3 炫酷图标与动画 ✅
- [x] Element Plus Icons 替换全部 emoji
- [x] CountUp 数字滚动组件 (easeOutExpo 缓动)
- [x] 卡片 hover: translateY(-4px) + scale(1.02) + 大阴影
- [x] 热度>80 板块脉冲动画 (pulse 2s 无限循环)
- [x] 排名徽章: 前3名金/银/铜渐变
- [x] 板块热度值紫蓝渐变文字

### M6: 定时任务 + 通知 🟢
- [ ] 定时分析接入引擎 (`scheduler.py` 调用 `run_full_analysis`)
- [ ] 交易日判断 (跳过节假日)
- [ ] 飞书通知

### M7: 性能优化 🟢
- [ ] 全量评分 (当前仅100只 → 全量5400+)
- [ ] 评分引擎性能优化 (并行/缓存)
- [ ] Redis 缓存层

---

## 执行优先级

```
M6 (定时任务) → M7 (性能优化)
```

## 每次迭代流程
1. 明确当前模块任务
2. 编码 → 测试 → 重启验证
3. Git 提交
4. 汇报进度

---

*文档版本: v4.0 | 更新日期: 2026-05-05*
